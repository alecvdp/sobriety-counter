# Core dependencies (optional - app works without them with reduced features)
plyer>=2.1.0        # Desktop notifications (cross-platform)
pystray>=0.19.0     # System tray icon (cross-platform)
Pillow>=10.0.0      # Image support for tray icon

# Development / packaging
# pyinstaller>=6.0   # Uncomment to install PyInstaller for building executables
